runs:
  using: composite
  steps:
    - uses: actions/setup-node@1a4442cacd436585916779262731d5b162bc6ec7 # v3
      with:
        node-version: 18
    - name: Build UI
      shell: bash
      run: |
        set -euxo pipefail
        cd src/ui
        npm ci --no-audit
        npm run build:prod
